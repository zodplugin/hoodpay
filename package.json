{
  "name": "@zellify/hoodpay",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "devDependencies": {
    "@zellify/tsconfig": "workspace:*"
  },
  "dependencies": {
    "ky": "^1.5.0"
  },
  "scripts": {
    "build": "tsc --build --force tsconfig.json",
    "build:watch": "tsc --build --force ./tsconfig.json --watch",
    "post-install": "pnpm build"
  }
}